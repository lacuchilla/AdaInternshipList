<h1>Here Are All The People!</h1>
  <ul>
  <% @people.each do |person| %>
    <li><%= link_to "#{person.name}", "/companies/#{@company.id}/people/#{person.id}/" %></li>
  <% end %>
  </ul>
  <%= link_to "Add a person", new_person_path %>
