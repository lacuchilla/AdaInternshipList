<% if !session[:user_id].nil? %>
  <br>
  <%= flash[:login] %>
<% end %>
<h2>Cohort 4</h2>
<h3>List of Sponsor Companies</h3>
  <ul>
    <% @companies.each do |company| %>
      <li><%= link_to "#{company.name}", company_path(:id => company.id) %></li>
    <% end %>
  </ul>

  <%= link_to "Add a Company", "/companies/new" %>
